- Edit the first 3 lines in `hyprland.conf` to preferences.

- Configurations for Hyprland is kept in `hyprconfs`.

- `pics` is the wallpaper directory.

- `scripts` stores bash scripts for many uses.
